/* Reset some default spacing */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

/* Hero full-screen image */
.hero {
  background-image: url('Banned book.jpg');
  background-size: cover;
  background-position: center;
  height: 100vh;
  position: relative;
  color: white;
  display: flex;
  justify-content: center;
  align-items: center;
  text-align: center;
}

.hero::after {
  content: "";
  position: absolute;
  top: 0; left: 0; right: 0; bottom: 0;
  background: rgba(0, 0, 0, 0.4); /* dark overlay */
}

.hero-text {
  position: relative;
  z-index: 1;
}

.hero-text h1 {
  font-size: 4rem;
  letter-spacing: 3px;
}

.hero-text p {
  font-size: 1.5rem;
  margin-top: 10px;
}

.arrow {
  margin-top: 30px;
  width: 40px;
  height: 40px;
  cursor: pointer;
}

/* Info section that fades in as you scroll */
.info {
  padding: 80px 20px;
  background-color: #fff;
  text-align: center;
}

.info h2 {
  font-size: 2rem;
  margin-bottom: 20px;
}

.info p {
  font-size: 1.1rem;
  margin-bottom: 15px;
  max-width: 800px;
  margin-left: auto;
  margin-right: auto;
}


